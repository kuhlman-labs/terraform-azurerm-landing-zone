name: $(Date:yyyyMMdd)$(Rev:.r)

trigger:
- master

stages:
- stage: Install_Terraform
  displayName: Install Terraform 0.11.14 on build agent and add to $PATH
  jobs:
  - job: Installing_Terraform
    pool:
      vmImage: 'ubuntu-16.04'
    steps:
    - script: sudo ./scripts/install_terraform.sh

    #- script: wget https://releases.hashicorp.com/terraform/0.11.14/terraform_0.11.14_linux_amd64.zip
    #- script: unzip terraform_0.11.14_linux_amd64.zip
    #- script: sudo mv terraform /usr/local/bin/
    - script: terraform --version 